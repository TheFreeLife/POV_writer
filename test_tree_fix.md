# 파일 트리 수정 사항

## 문제
1. 폴더가 증식하는 문제 - renderFileTree가 호출될 때마다 이벤트 리스너가 중복 등록됨
2. 순서가 제대로 안 바뀜 - 순서 재정렬 로직 문제

## 해결 방법
1. 컨테이너 드롭 이벤트를 loadProjectFiles에서 한 번만 등록
2. moveFile 로직을 단순화하고 순서 재정렬 개선

## 수정 필요한 moveFile 함수
- 같은 부모 내에서 순서 변경 시 로직 개선
- 다른 부모로 이동 시 순서 맨 마지막으로
